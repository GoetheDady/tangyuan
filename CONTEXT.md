# 汤圆

汤圆是一个面向大语言模型对话的桌面应用。本文记录产品和领域语言，帮助界面、运行配置和会话体验保持一致。

## Language

**Agent**:
独立 AI 实体，拥有自己的身份设定、技能、工作空间、会话集合和模型选择。同一用户可以拥有多个 Agent；所有 Agent 共享同一份用户画像和按模型服务保存的凭据。
_Avoid_: 助手实例、模型、聊天角色

**Agent 灵魂**:
描述单个 Agent 长期身份、职责、行为规则、沟通方式和权限边界的信息。每个 Agent 拥有各自独立的 Agent 灵魂。
_Avoid_: Agent 规则、Agent 画像、身份配置

**默认 Agent**:
应用内置且不能归档的“汤圆”Agent，是用户配置模型服务凭据和创建其他 Agent 的唯一入口。
_Avoid_: 管理员 Agent、系统 Agent、主 Agent

**用户画像**:
描述当前唯一用户的长期偏好、工作方式和边界，并由所有 Agent 共享的信息。
_Avoid_: 用户资料、Agent 用户信息、用户账号、每 Agent 用户画像

**Agent 创建**:
用户通过默认 Agent 的对话描述需求，由默认 Agent 调用受控工具生成新 Agent 和初始身份设定的过程。创建完成的新 Agent 可以立即开始普通会话。
_Avoid_: 创建表单、手动建目录

**Agent 归档**:
将 Agent 从日常使用列表中移除，但保留其身份、会话和工作空间的可恢复状态。
_Avoid_: 删除 Agent、永久删除

**工作空间**:
每个 Agent 独立拥有的本地文件区域，用于保存该 Agent 工作时读取或产出的文件。MVP 中一个 Agent 只有一个工作空间，不与其他 Agent 共享。
_Avoid_: Agent Home、项目目录、共享目录

**Agent 命令许可**:
用户对某个 Agent 的某条命令在某个工作目录下作出的长期审批决定；该决定对该 Agent 的所有会话生效，但不跨 Agent，也不跨工作目录。
_Avoid_: 当前会话免审、全局命令许可、无条件永久允许

**命令风险判断**:
Agent 结合命令、参数、工作目录和任务目的判断一次命令执行的风险，并向用户解释判断结果；Runtime 依据当前生效的高风险策略提供兜底，可以提高风险等级，但不能降低 Agent 已声明的风险等级。
_Avoid_: 纯字符串匹配、Runtime 全权判断、Agent 自我审批

**内置默认高风险策略**:
汤圆默认提供的高风险命令规则集合。当前由应用提供；未来允许用户在明确理解风险后修改，并能够恢复为默认规则。
_Avoid_: 不可修改安全红线、Agent 临时风险判断、永久硬编码清单

**高风险命令**:
被 Agent 判断为高风险，或被当前生效的高风险策略认定为高风险的命令；它可以被允许本次执行，但每次都必须由用户手动确认，不能被加入 Agent 命令许可。
_Avoid_: 永久允许命令、自动批准命令

**本地目录布局**:
把逻辑概念（某 Agent 的 Home、工作空间、Agent 专属 Skill 目录、共享 Skill 目录、用户画像、某会话文件、配置文件）翻译成本地磁盘路径的一套约定。它集中承载汤圆本地文件的组织方式，是这些概念的路径解析器，不代表存储引擎或数据本身。
_Avoid_: 路径工具、fs 封装、存储引擎、数据库

**共享 Skill**:
安装一次并对所有 Agent 可用的能力说明与配套资源。
_Avoid_: 全局插件、公共脚本

**Agent 专属 Skill**:
只对指定 Agent 可用的能力说明与配套资源；与共享 Skill 同名时，专属版本生效。
_Avoid_: 私有插件、Agent 脚本

**黑芝麻汤圆主题**:
汤圆的统一极简视觉语言，以糯米白、瓷白和黑芝麻墨黑为主体，并用少量朱砂红和状态色表达重点。它依靠留白、排版和对齐呈现安静、清晰的桌面工作感，不依靠装饰堆叠或餐饮意象。
_Avoid_: 米黄色主题、棕色餐饮风、纯黑白主题、渐变发光、玻璃拟态、卡片堆叠

**聊天主界面**:
用户与大语言模型进行连续对话的主要工作区。它只承载会话列表、消息流、输入与运行中的对话动作。
_Avoid_: 工作台、控制台、运行时面板

**Agent 导航栏**:
聊天主界面最左侧用于切换当前 Agent、进入设置等全局导航的窄栏。
_Avoid_: Agent 列表、Agent rail、左侧栏

**会话列表栏**:
聊天主界面中用于展示当前 Agent 的会话集合、会话谱系和新建会话入口的栏位。
_Avoid_: 会话侧边栏、中间栏、Session pane

**对话区**:
聊天主界面中用于展示当前会话标题、消息流、对话动作和输入区的主要区域。
_Avoid_: 会话区域、聊天面板、右侧栏

**设置页面**:
与聊天主界面和初始化配置页面分离的管理页面，用于维护共享模型服务凭据、各 Agent 的模型选择、状态、Skills 和归档。
_Avoid_: 控制台页面、运行时配置页面、设置弹窗、调试面板

**初始化配置页面**:
模型服务尚未满足启动对话条件时，用户配置 Provider、凭据和默认 Agent 初始模型的独立页面。
_Avoid_: 设置页面、欢迎页、身份初始化页面

**窗口拖拽区**:
横跨所有页面顶部、承载窗口拖拽并为系统窗口控件预留位置的公共区域。它不承载业务内容与导航，只保证任何页面下用户都能拖动窗口。
_Avoid_: 标题栏、Agent rail 拖拽、顶部工具栏

**配置阻断**:
因模型服务、模型或凭据尚未满足启动对话条件，应用临时阻止用户进入聊天主界面并导航到初始化配置页面的状态。配置完成后应回到用户原本要继续的对话目标；没有明确目标时进入新对话。
_Avoid_: 错误页、运行时控制态

**初始化阻断**:
默认 Agent 的 `bootstrap.md` 仍存在时，汤圆持续补问初始化信息并暂不处理普通任务的状态。该状态不绑定某个特殊会话，删除 `bootstrap.md` 后结束。
_Avoid_: bootstrap session、首次会话、身份初始化页面

**配置入口**:
用户从聊天主界面进入设置页面的导航入口。它只负责页面导航，不在聊天主界面直接展开模型或凭据控件。
_Avoid_: 运行时控制、接口密钥按钮

**对话消息**:
用户发送的内容和大语言模型面向用户的回复内容。Agent 回复的对话消息就是其最终回复，始终独立于执行历史展示，不随执行历史的展开或收起而出现或消失。聊天主界面的消息流以对话消息为主，并可以展示与 Agent 回复相关联的执行历史；执行历史本身不属于对话消息。
_Avoid_: transcript 全量消息、活动消息、系统消息

**Agent 执行历史**:
Agent 为形成一次回复而产生并向用户公开的过程记录，按回合分段、回合内再按步骤展开，包括思考内容、中间文字、工具调用及其结果。执行历史与对应的 Agent 回复相关联，使用户能够查看任务如何推进，但不等同于会话中的对话消息。思考步骤保留 Runtime 提供的原文，不改写为摘要；工具步骤以用户可理解的摘要表达，不把完整工具参数或原始输出作为执行历史正文。执行历史中的文字步骤只承载中间文字；构成最终回复的末回合文字不作为执行步骤出现在执行历史里，以免与对话消息重复。
_Avoid_: 系统消息、调试日志、原始工具日志、transcript 全量消息

**上下文压缩提示**:
会话自动压缩上下文时，在消息流对应位置展示的轻量系统提示。它帮助用户理解上下文边界的变化，但不属于对话消息或 Agent 执行历史。
_Avoid_: 压缩消息、Agent 回复、执行步骤

**响应等待提示**:
Agent 执行尝试已开始但尚未产生任何可见内容时，在消息流末尾展示的轻量指示。首个思考、文字或工具步骤到达即被取代；执行尝试失败或被取消时消失。它帮助用户确认「Agent 正在响应」，但不属于对话消息或 Agent 执行历史。
_Avoid_: 空回复、思考步骤、加载骨架、上下文压缩提示

**对话动作**:
Agent 运行期间需要用户处理后才能继续的交互，包括命令审批和问题澄清。一次澄清只提出一个问题；需要继续确认时，由 Agent 依次提出后续问题。等待处理时，对话动作以可操作形式出现，并阻止用户发送新的普通消息；处理完成后失去交互能力，并作为 Agent 执行历史的一部分保留用户的选择结果。
_Avoid_: 对话消息、设置表单、一次性弹窗

**执行尝试**:
Agent 针对一条用户消息进行的一次完整处理过程，具有独立的执行历史和完成、失败或中断结果。同一条用户消息可以在失败后发起新的执行尝试；新的尝试不重复产生用户消息，先前失败的尝试仍可回看。多次执行尝试分别展示，不把各次历史或结果合并。一次执行尝试由一个或多个回合组成。
_Avoid_: 重复消息、续写、覆盖重试

**回合**:
一次执行尝试内的一次完整大模型生成循环：模型产出一段回应（可能含思考与面向用户的文字），并执行由这段回应触发的一批工具调用。模型拿到工具结果后继续生成就进入下一回合；最后一个回合通常不含工具，只给出最终答复。回合边界与序号由 Runtime 依据底层 SDK 给出，不由上层自行推断。
_Avoid_: 轮次、生成轮、工具循环、LLM turn

**步骤**:
一个回合内、按 Agent 实际产生顺序排列的一个原子过程单元，分为思考步骤（模型推理原文）、文字步骤（面向用户的中间文字）和工具步骤（一次工具调用及其结果摘要）。同一回合内思考与文字可交错，均排在工具批次之前；工具步骤按启动顺序排列。
_Avoid_: 活动、事件、条目

**会话**:
用户与 Agent 进行一次连续对话的独立上下文，包含自身的消息历史和基本描述信息。Pi SDK 以树形结构存储会话中的消息：每条消息是一个节点，用户可以从任意历史节点分叉出新的回复路径。
_Avoid_: 聊天记录、对话文件

**会话标识**:
唯一标识一个会话的稳定身份，由 Pi SDK 生成，并同时作为汤圆索引、IPC、路由和侧边栏使用的 sessionId。一个会话不维护第二套应用标识。
_Avoid_: 本地会话 ID、映射 ID、显示序号

**最后激活会话**:
用户最后一次打开的会话及其所属 Agent。应用启动时优先恢复它；不可用时才回退到默认 Agent 的可用最近会话或新会话。
_Avoid_: 最近创建会话、默认会话、全局当前会话

**分叉**:
从会话历史的一条用户消息创建新的独立会话。分叉会话保留该消息之前的上下文，并以该消息文本作为可编辑草稿；原会话不受影响，两个会话可以独立切换、比较和运行。
_Avoid_: 同文件分支、leaf 切换、另存为

**分叉会话**:
由分叉生成、拥有独立 Pi session 文件和运行状态的会话。它可以与父会话并行运行，但生命周期依赖完整的父会话链；任一祖先不可用时，它也不可用。
_Avoid_: 逻辑分支、可孤立会话、共享 session 分支

**会话运行配置**:
某个会话当前生效的 Provider、Model 与 Thinking Level。分叉会话在创建时继承父会话的会话运行配置，之后两个会话分别维护。
_Avoid_: Agent 默认配置、全局模型设置、Provider 配置

**会话谱系**:
原会话与全部分叉会话形成的父子关系树，也是会话删除与可用性判断的生命周期边界。删除其中一个会话会连带删除它的全部后代；任一祖先不可用时，其后代也不可用。
_Avoid_: 消息树、互不相关的会话集合、分支文件夹

**会话归档**:
将一个会话及其全部后代从日常会话列表隐藏、但保留其全部 Pi session 文件和索引的可恢复操作。归档前必须停止该子树的活动任务；恢复时整个已归档子树一同恢复。
_Avoid_: 删除会话、隐藏单个节点、永久清除

**会话删除**:
永久清除一个会话及其全部后代的 Pi session 文件与索引的不可恢复操作。删除前必须停止该子树的活动任务；它不影响祖先或兄弟会话。
_Avoid_: 归档会话、移出侧边栏、清理当前路径

**分叉源消息**:
用户选中并据此创建分叉会话的用户消息。分叉会话复制该消息父节点之前的上下文；若它是首条用户消息，则从空上下文开始，并把其文本作为可编辑的预填草稿。分叉源消息本身不属于新会话的历史。
_Avoid_: 分叉节点、分支起点、被复制的消息

**分叉来源提示**:
仅在分叉会话消息流顶部展示的导航信息，说明其父会话与分叉源消息，并允许用户跳回来源。它不属于会话消息，也不写入 Pi session 文件。
_Avoid_: 系统消息、会话首消息、分叉摘要

**分叉来源记录**:
随分叉会话持久化的来源关系，包含父会话与分叉源消息，使会话索引重建后仍能恢复会话谱系和分叉来源提示。它不参与模型上下文。
_Avoid_: UI 提示、会话消息、仅索引元数据

**会话节点**:
会话树中的一条消息（用户消息或 Agent 回复），由唯一 entryId 标识。会话树从根节点开始，沿当前活跃分支延伸到叶子节点；每个非叶子节点可以有一个或多个子节点（即分叉点）。
_Avoid_: 消息索引、对话行、树节点

**会话索引**:
汤圆基于 Pi session 文件建立的本地会话目录，负责记录会话归属、展示信息、会话谱系和产品扩展数据。它是可从 Pi session 重建的派生信息，不替代 Pi session 作为会话内容的唯一真相。
_Avoid_: 会话数据库、第二消息存储、Pi session 替代品

**会话扩展数据**:
汤圆附加在现存会话上的产品专属信息。它不属于会话自身的内容或基本描述信息，也不能脱离所属会话独立存在。
_Avoid_: 会话元数据、孤立扩展数据
