# 本地优先的单用户 MVP

汤圆的 MVP 定位为本地优先的单用户桌面应用：Agent、会话和运行配置均由本机持有，不建设账号体系、云同步、多人协作或远程服务端。这样可以先验证配置模型服务、维护 Agent 和连续对话这条核心链路，避免尚未验证产品价值时引入认证、同步冲突和远程数据治理成本。

单用户不等于单 Agent。MVP 允许该用户创建和使用多个相互隔离的 Agent；“单用户”只排除账号、团队成员和权限协作，不限制本地 Agent 数量。
