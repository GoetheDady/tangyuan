# 对账 Agent 注册表与本地目录

`config.json` 是 Agent 注册表事实来源，Agent Home 是其持有的数据目录。配置存在但目录缺失时，将 Agent 标记为损坏并禁止聊天，不能静默重建身份；目录存在但配置没有对应 Agent 时，将其标记为未认领目录，由控制台在用户确认后恢复导入。

默认 Agent“汤圆”是例外：固定目录缺失时可按内置模板重建。任何会移动、导入或重建数据的恢复操作都必须先展示影响并获得用户确认。
