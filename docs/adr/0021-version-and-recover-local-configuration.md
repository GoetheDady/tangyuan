# 为本地配置提供版本迁移与恢复

`config.json` 顶层包含 `schemaVersion`，应用启动时按顺序执行显式迁移，将旧结构转换为当前结构。配置写入使用临时文件和原子替换，写前保留一个最近可恢复备份；API Key 密文只在 Main 进程中随配置迁移。

配置解析或迁移失败时不得覆盖原文件，聊天进入配置阻断，控制台显示配置恢复状态，允许用户恢复备份或重置配置。重置只重建配置，不删除 Agent Home、workspace、共享用户资料或 Pi session。
